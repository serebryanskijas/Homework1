package homework7;

import java.text.ParseException;

enum RenaultModels{
    SYMBOL, SCENIC, LOGAN, KADJAR, MEGANE
}
public class Renault extends Car{
    private RenaultModels model;

    Renault(String sDate, double price, double averageFuelConsumption, int maxSpeed, CarColors color) throws ParseException {
        super(sDate, price, averageFuelConsumption, maxSpeed, color);
        this.model = model;
        this.
    }
}
